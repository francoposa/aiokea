# Generalized common keys

# For pulling registered background tasks down from the app
RUNNING_TASKS = "running_tasks"

# For pulling the HTTP Request <-> Usecase adapter down from the app
HTTP_ADAPTER = "http_adapter"

# For pulling the UsecaseDatabaseClient down from the app
DATABASE_CLIENT = "database_client"
